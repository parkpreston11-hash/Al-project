[build]
  command = "pnpm install && pnpm --filter @workspace/realty-site run build"
  publish = "artifacts/realty-site/dist/public"

[build.environment]
  NODE_VERSION = "20"
  BASE_PATH = "/"
  PORT = "3000"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
